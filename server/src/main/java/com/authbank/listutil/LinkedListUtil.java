package com.authbank.listutil;

/***
 * @author 沙建斌
 * @date 2021-11-07
 * @version  1.0
 * @description LinkedList
 *      LinkedList 具有以下特征:
 *              1:LinkedList是大小可变的数组实现.即按照添加的顺序存储。
 *              2:允许包含一条或多条Null值存在.
 *              3:ArrayList是不同步，即当至少两条线程访问时至少一条线程从结构上修改列表,那么他必须保持对外一致(结构上的修改
 *              指的是任何添加或者删除一个或多个列表元素的操作,)如果想要
 *              避免不同步导致的问题一般是通过对自然封装该列表的对象进行同步操作来完成,如果不存在这样的对象,则应该在创建该ArrayList
 *              时使用Collection.synchronizedList方法将该列表包装起来，以防止意外对列表的不同步访问
 *                  List list = Collection.synchronizedList(new LinkedList());
 * ***/
public class LinkedListUtil {
}
